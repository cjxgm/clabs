# MacOS X version

# Setup colorsets
Read $./Colorsets

DestroyDecor recreate WindowDecoration
AddToDecor WindowDecoration

+ TitleStyle Active TiledPixmap $./window-active-title.png
+ TitleStyle InActive TiledPixmap $./window-inactive-title.png
+ TitleStyle LeftJustified Height 17 -- flat

+ BorderStyle -- HiddenHandles
+ BorderStyle -- Flat

+ ButtonStyle 1 \
	Active (Pixmap $./window-active-close-left.png) \
	Inactive (Pixmap $./window-inactive-close-left.png)

+ ButtonStyle 2 \
	Active (Pixmap $./window-active-empty-right.png) \
	Inactive (Pixmap $./window-inactive-empty-right.png)

+ ButtonStyle 3 \
	Active (Pixmap $./window-active-iconify.png) \
	Inactive (Pixmap $./window-inactive-iconify.png)

+ ButtonStyle 5 \
	Active (Pixmap $./window-active-maximize.png) \
	Inactive (Pixmap $./window-inactive-maximize.png)

+ ButtonStyle All -- UseTitleStyle
+ ButtonStyle All -- flat

Style * HandleWidth 1, BorderWidth 1
Style * Colorset                $[cs_window_inactive], \
        BorderColorset          $[cs_window_inactiveborder], \
        HilightColorset         $[cs_window_active], \
        HilightBorderColorset   $[cs_window_activeborder]

Style * Font "xft:Verdana:pixelsize=11"

Style * UseDecor WindowDecoration

# Setup button functions
Include components/decorations/Buttons-macosx

